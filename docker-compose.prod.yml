version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    image: modellens/backend:latest
    container_name: modellens-backend
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - GIN_MODE=release
    networks:
      - modellens-network
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  modellens-network:
    driver: bridge
